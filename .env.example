# Omni Shop environment variables
# Copy to .env.local and fill in real values.

# Required for AI search intent parsing (/api/search-intent)
OPENAI_API_KEY=

# Site access password (server-only; never expose to the client)
SITE_ACCESS_PASSWORD=

# Secret used to sign the HttpOnly session cookie
AUTH_SECRET=
